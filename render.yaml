services:
  - type: web
    name: jaipur-dream-homes
    env: node
    plan: free
    buildCommand: bun install && bun run build
    startCommand: bun start
    envVars:
      - key: NODE_ENV
        value: production
